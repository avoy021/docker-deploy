version : "3"
services : 
  nginx:
    ports:
      - "80:80"
  app1:
    build:
      context: .
      args:
        NODE_ENV: production
    environment:
      - NODE_ENV=production
    command: npm run start 